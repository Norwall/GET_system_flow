[pytest]
testpaths = tests
addopts = -p no:cacheprovider
markers =
    slow: computationally expensive steady-state or distributed model scenarios
    distributed: distributed steady-state profile and regime-aware closure scenarios
